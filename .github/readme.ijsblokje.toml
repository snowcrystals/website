[project]
icon = "https://raw.githubusercontent.com/snowcrystals/.github/main/logo.png"
icon_width = "100px"
version = "1.0.0"

[variables]
information = "Design stolen from [Discord.js](https://discordjs.dev). Why? Because we liked it! \"Beter goed gestolen dan slecht bedacht\" - Some Dutch guy"
installation = "Install the dependencies using `pnpm install`. Build using `pnpm build`, start using `pnpm run start --filter=web`. Run development mode using `pnpm start:dev`."